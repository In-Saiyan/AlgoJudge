# =============================================================================
# Vanguard API Gateway Configuration
# =============================================================================

# Server
PORT=8081
ENVIRONMENT=development

# Database (PostgreSQL)
DATABASE_URL=postgres://olympus:olympus_dev@localhost:5432/olympus

# Redis
REDIS_URL=redis://localhost:6379

# JWT Authentication
JWT_SECRET=change-this-in-production
JWT_ACCESS_EXPIRATION=900       # 15 minutes (in seconds)
JWT_REFRESH_EXPIRATION=604800   # 7 days (in seconds)

# =============================================================================
# Docker Compose Port Mappings
# =============================================================================
# Change these if you have port collisions

# PostgreSQL (default: 5432)
POSTGRES_PORT=5432

# Redis (default: 6379)
REDIS_PORT=6379

# Prometheus (default: 9090)
PROMETHEUS_PORT=9090

# Grafana (default: 3001)
GRAFANA_PORT=3001
